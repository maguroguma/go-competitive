snippet     digitDP
abbr        dp[0][0] = 1
options     head
  // 初期化: dp[0][0] = 1

  for i := 0; i < len(${1:S}); i++ {
    digit := int($1[i] - '0')
    for j := 0; j < ${2:10}; j++ {
      // less確定の遷移: dp[i+1][1] <- dp[i][1]

      // less未確定の遷移
      if j < digit {
        // dp[i+1][1] <- dp[i][0]

      } else if j == digit {
        // dp[i+1][0] <- dp[i][0]

      }
    }
  }

snippet     kiriage
abbr        (a + (b - 1)) / b
options     head
  (${1:#:bunshi} + (${2:#:bunbo} - 1)) / $2

snippet     kiriageFunc
abbr        func Kiriage(a, b int) int
options     head
  // Kiriage returns Ceil(a/b)
  // a >= 0, b > 0
  func Kiriage(a, b int) int {
    return (a + (b - 1)) / b
  }

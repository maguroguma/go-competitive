snippet     meguruBinarySearch
abbr        ng, ok := -1, len(A)
options     head
  ${1:isOK} := func(m int) bool {
    return false
  }

  ng, ok := -1, len(${2:#:SliceName})
  for int(math.Abs(float64(ok - ng))) > 1 {
    mid := (ok + ng) / 2
    if $1(mid) {
      ok = mid
    } else {
      ng = mid
    }
  }
  ${3:boundaryIdx} := ok


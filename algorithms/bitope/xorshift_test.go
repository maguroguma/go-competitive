package bitope

import (
	"testing"
)

func TestXorshift(t *testing.T) {
	// count := [6]int{}
	// ratio := [6]float64{}
	// num := 1000000

	// for i := 0; i < num; i++ {
	// 	rng := RandInt() % 6
	// 	count[rng]++
	// }

	// for i := 0; i < 6; i++ {
	// 	ratio[i] = float64(count[i]) / float64(num)
	// }

	// for i := 0; i < 6; i++ {
	// 	assert.True(t, math.Abs(ratio[i]-float64(1)/float64(6)) < 0.1)
	// }
}
